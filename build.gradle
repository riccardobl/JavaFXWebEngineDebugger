apply plugin:  'maven'
apply plugin: 'java'

group = "javafx"
version = "dev"

repositories {
    jcenter()
}

dependencies {
	compileOnly "org.java-websocket:Java-WebSocket:1.3.0"
	testCompile 'com.google.code.gson:gson:2.8.0'
}
